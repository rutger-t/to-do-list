<% provide(:title, "New") %>

<h1><%= t "new.title" %></h1>

    <%= form_for(@task) do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :description %>
      <%= f.text_field :description %>

      <%= f.submit %>
    <% end %>

    &nbsp;
    
    <p> <%= link_to t("links.home"), root_path, class: "btn btn-primary btn-md" %></p>
    